商品一覧

<% @item.each do |item| %>
<%= image_tag item.image.variant(resize: '300x300') %>
<%= item.name %>
<%= item.explanation %>
<%= item.category.name %>
<%= item.price %>
<% end %>

<%= link_to "トップページ" ,root_path %>